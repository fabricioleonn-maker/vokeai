---
description: Autonomy workflow for VokeAI/Bingola (Turbo Mode)
---

// turbo-all
1. Check project status
   `git status`
2. List directory contents
   `dir`
3. Check node environment
   `node -v && npm -v`

This workflow enables high autonomy for this project.
